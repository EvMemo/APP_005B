<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 点击之后的color -->
    <item android:color="@color/buttonSelect" android:textColor="@color/buttonSelect" android:state_activated="true" />
    <!-- 未点击或再次点击之后的color -->
    <item android:color="@color/buttonNoSelect" android:state_activated="false" />
</selector>